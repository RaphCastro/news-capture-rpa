robot:
  name: News Scraper
  description: A bot that scrapes news articles from AlJazeera and Latimes.

tasks:
  Default:
    command:
      - python3 -m pip install -r src/requirements.txt  # Install dependencies from requirements.txt
      - python3 src/main.py  # Run your Python script

artifactsDir: output
