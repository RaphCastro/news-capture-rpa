robot:
  name: News Scraper
  description: A bot that scrapes news articles from AlJazeera and Latimes.

tasks:
  Default:
    command:
      - python3
      - src/main.py

artifactsDir: output

condaConfigFile: conda.yaml
