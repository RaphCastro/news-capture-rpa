robot:
  name: News Scraper
  description: A bot that scrapes news articles from AlJazeera and Latimes.

tasks:
  Default:
    command:
      - python3
      - -m pip install -r src/requirements.txt
      - src/main.py

artifactsDir: output