robot:
  name: News Scraper
  description: A bot that scrapes news articles from AlJazeera and Latimes.

condaConfigFile: conda.yaml  # Use the conda.yaml file for environment setup

tasks:
  Default:
    command:
      - python
      - src/main.py  # Run your Python script

artifactsDir: output
