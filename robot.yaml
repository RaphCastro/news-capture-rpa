robot:
  name: News Scraper
  description: A bot that scrapes news articles from AlJazeera and Latimes.

tasks:
  Default:
    command:
      - python3
      - -m
      - pip
      - install
      - -r
      - src/requirements.txt  # Path to your requirements.txt file
      - &&  # Use '&&' to chain commands on the same line
      - python3 
      - src/main.py  # Run your Python script

artifactsDir: output
